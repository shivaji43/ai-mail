# Copy this file to .env.local and fill in your values

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Google OAuth Configuration
# IMPORTANT SETUP STEPS:
# 1. Enable Gmail API in Google Cloud Console
# 2. Add these scopes to your OAuth consent screen:
#    - https://www.googleapis.com/auth/gmail.readonly (includes metadata access)
#    - https://www.googleapis.com/auth/gmail.send
#    - https://www.googleapis.com/auth/gmail.compose
#    - https://www.googleapis.com/auth/gmail.modify
#    - https://www.googleapis.com/auth/gmail.labels
# 3. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (for development)
#    - https://yourdomain.com/api/auth/callback/google (for production)
# 4. If you get 403 errors, users may need to sign out and sign in again
#    to get the new scopes
# 5. Test Gmail API access at: /api/gmail-test
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Gmail Push Notifications Configuration (Cloud Pub/Sub)
# IMPORTANT: Replace 'zero-455106' with your actual Google Cloud Project ID
# This enables real-time email notifications instead of polling
# See GMAIL_PUSH_SETUP.md for detailed setup instructions
NEXT_PUBLIC_GMAIL_TOPIC_NAME=projects/zero-455106/topics/gmail-notifications

# Optional: Webhook verification token for additional security
# This can be any random string to verify webhook authenticity
GMAIL_WEBHOOK_TOKEN=your-secret-webhook-token-here

# Optional: Database Configuration
# Uncomment if you plan to store user data, watch states, email cache, etc.
# DATABASE_URL=postgresql://username:password@localhost:5432/ai_mail
# DATABASE_URL=mysql://username:password@localhost:3306/ai_mail
# DATABASE_URL=sqlite:./dev.db

# Optional: Redis Configuration for caching
# Uncomment if you want to use Redis for email caching instead of memory/localStorage
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your-redis-password

# Optional: Logging and Monitoring
# LOG_LEVEL=info
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Optional: Rate Limiting Configuration
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW=900000

# Optional: Email Content Processing
# MAX_EMAIL_SIZE=10485760
# ALLOWED_DOMAINS=gmail.com,googlemail.com

# Optional: Development Configuration
# DEBUG_GMAIL_API=true
# SKIP_GMAIL_WATCH=false 